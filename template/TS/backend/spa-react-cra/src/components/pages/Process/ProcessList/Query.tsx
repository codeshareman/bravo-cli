import React, { FC } from 'react';
import styled from 'styled-components';

// 可用流程列表搜索
const Query: FC = () => {
  return <Conatiner>{/* 搜索 */}</Conatiner>;
};

const Conatiner = styled.div``;

export default Query;
